package part1basics

object ValuesVariablesTypes extends App {
  // "hello world" is a string, println("hello world") is a side ffect (will print to console) and will return unit.
  // someValue = true
  // someOtherValue = 42
}